<section class="value-props">
  <div class="page-width value-props__grid">
    {% for block in section.blocks %}
      <article class="value-prop-card" {{ block.shopify_attributes }}>
        <span class="value-prop-card__icon">{{ block.settings.icon }}</span>
        <div>
          <p class="value-prop-card__eyebrow">{{ block.settings.eyebrow }}</p>
          <h3>{{ block.settings.heading }}</h3>
          <p>{{ block.settings.text }}</p>
        </div>
      </article>
    {% endfor %}
  </div>
</section>

{% schema %}
{
  "name": "Value props",
  "max_blocks": 4,
  "blocks": [
    {
      "type": "item",
      "name": "Value prop",
      "settings": [
        {
          "type": "text",
          "id": "icon",
          "label": "Icon",
          "default": "✓"
        },
        {
          "type": "text",
          "id": "eyebrow",
          "label": "Eyebrow",
          "default": "Free delivery"
        },
        {
          "type": "text",
          "id": "heading",
          "label": "Heading",
          "default": "Included on every order"
        },
        {
          "type": "textarea",
          "id": "text",
          "label": "Text",
          "default": "No minimum spend. No checkout sting in the tail."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Value props",
      "blocks": [
        { "type": "item" },
        { "type": "item", "settings": { "icon": "£", "eyebrow": "Deals worth opening", "heading": "Fresh savings added regularly", "text": "The store stays stocked with smart buys, not filler." } },
        { "type": "item", "settings": { "icon": "↺", "eyebrow": "Human-friendly support", "heading": "Clear help if something is off", "text": "Need a hand with an order? You can get answers without a maze." } }
      ]
    }
  ]
}
{% endschema %}
