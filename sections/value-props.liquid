<section class="value-props">
  <div class="page-width value-props__grid">
    {% for block in section.blocks %}
      <article class="value-prop-card" {{ block.shopify_attributes }}>
        <span class="value-prop-card__icon">{{ block.settings.icon }}</span>
        <div>
          <p class="value-prop-card__eyebrow">{{ block.settings.eyebrow }}</p>
          <h3>{{ block.settings.heading }}</h3>
          <p>{{ block.settings.text }}</p>
        </div>
      </article>
    {% endfor %}
  </div>
</section>

{% schema %}
{
  "name": "Value props",
  "max_blocks": 4,
  "blocks": [
    {
      "type": "item",
      "name": "Value prop",
      "settings": [
        {
          "type": "text",
          "id": "icon",
          "label": "Icon",
          "default": "✓"
        },
        {
          "type": "text",
          "id": "eyebrow",
          "label": "Eyebrow",
          "default": "Fast shipping"
        },
        {
          "type": "text",
          "id": "heading",
          "label": "Heading",
          "default": "Delivery confidence"
        },
        {
          "type": "textarea",
          "id": "text",
          "label": "Text",
          "default": "Put dispatch, returns and payment reassurance close to shopping actions."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Value props",
      "blocks": [
        { "type": "item" },
        { "type": "item", "settings": { "icon": "£", "eyebrow": "Daily deals", "heading": "Clear offer mechanics", "text": "Give shoppers obvious routes into discounts, bundles and clearance lines." } },
        { "type": "item", "settings": { "icon": "↺", "eyebrow": "Returns", "heading": "Retail trust signals", "text": "Keep delivery and return confidence visible while customers compare products." } }
      ]
    }
  ]
}
{% endschema %}
