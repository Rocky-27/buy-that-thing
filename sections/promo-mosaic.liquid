<section class="promo-mosaic">
  <div class="page-width promo-mosaic__grid">
    {% for block in section.blocks %}
      <article class="promo-tile promo-tile--{{ forloop.index }}" {{ block.shopify_attributes }}>
        <div class="promo-tile__content">
          <p class="section-eyebrow">{{ block.settings.eyebrow }}</p>
          <h3>{{ block.settings.heading }}</h3>
          <p>{{ block.settings.text }}</p>
          {% if block.settings.link_label != blank and block.settings.link != blank %}
            <a class="button button--dark" href="{{ block.settings.link }}">{{ block.settings.link_label }}</a>
          {% endif %}
        </div>
        {% if block.settings.image != blank %}
          {{ block.settings.image | image_url: width: 1200 | image_tag: class: 'promo-tile__image', loading: 'lazy' }}
        {% endif %}
      </article>
    {% endfor %}
  </div>
</section>

{% schema %}
{
  "name": "Promo mosaic",
  "max_blocks": 3,
  "blocks": [
    {
      "type": "tile",
      "name": "Tile",
      "settings": [
        {
          "type": "text",
          "id": "eyebrow",
          "label": "Eyebrow",
          "default": "Start here"
        },
        {
          "type": "text",
          "id": "heading",
          "label": "Heading",
          "default": "Useful things, smart upgrades and the occasional why-not find."
        },
        {
          "type": "textarea",
          "id": "text",
          "label": "Text",
          "default": "Browse buys that make life easier, homes better or gift ideas a bit less predictable."
        },
        {
          "type": "text",
          "id": "link_label",
          "label": "Link label",
          "default": "See the edit"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Link"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Promo mosaic",
      "blocks": [
        { "type": "tile", "settings": { "eyebrow": "Start here", "heading": "Useful things, smart upgrades and the occasional why-not find.", "text": "Browse buys that make life easier, homes better or gift ideas a bit less predictable." } },
        { "type": "tile", "settings": { "eyebrow": "Worth a peek", "heading": "Interesting finds without the endless scrolling.", "text": "Skip the filler and head straight for the products that feel handy, surprising or weirdly perfect.", "link_label": "See the good ones" } },
        { "type": "tile", "settings": { "eyebrow": "Finish the job", "heading": "The extras that make the main buy feel properly sorted.", "text": "Find the add-ons, accessories and finishing touches that stop a good idea being half-done.", "link_label": "Complete the setup" } }
      ]
    }
  ]
}
{% endschema %}
