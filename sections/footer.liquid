<footer class="site-footer">
  <div class="page-width site-footer__grid">
    <div>
      <p class="footer-kicker">Buy That Thing</p>
      <h2>Home, garden, tech and everyday essentials.</h2>
      <p class="footer-copy">Shop useful finds, regular offers and free shipping on every order.</p>
    </div>

    <div>
      <p class="footer-heading">Shop</p>
      <ul class="footer-links">
        {% for link in section.settings.menu.links %}
          <li><a href="{{ link.url }}">{{ link.title }}</a></li>
        {% endfor %}
      </ul>
    </div>

    <div>
      <p class="footer-heading">Support</p>
      <ul class="footer-links">
        <li><a href="{{ routes.search_url }}">Search</a></li>
        <li><a href="{{ routes.account_url }}">Account</a></li>
        <li><a href="{{ routes.cart_url }}">Cart</a></li>
      </ul>
    </div>
  </div>

  <div class="page-width site-footer__meta">
    <p>&copy; {{ 'now' | date: '%Y' }} {{ shop.name }}</p>
    <p>Secure checkout. Free shipping on every order.</p>
  </div>
</footer>

{% schema %}
{
  "name": "Footer",
  "settings": [
    {
      "type": "link_list",
      "id": "menu",
      "label": "Footer menu"
    }
  ]
}
{% endschema %}
